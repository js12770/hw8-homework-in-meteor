 Flask 也可以生成 URL
 url_for() 函数就是用于构建指定函数的 URL 的。它把函数名称作为 第一个参数，其余参数对应 URL 中的变量。未知变量将添加到 URL 中作为查询参数。

test_request_context() 方法
这个 方法的作用是告诉 Flask 我们正在处理一个请求

HTTP （ web 应用使用的协议）) 协议中有访问 URL 的不同方法。缺省情况下，一个路由 只回应 GET 请求，但是可以通过 methods 参数使用不同方法。

请求对象
操作 URl提交的参数可以使用 args 属性 searchword = request.args.get('key', '')

使用 errorhandler() 装饰器可以定制出错页面: